function makeNegative(num) {
  // Code?
    if (num > 0 ) {
        return -num
    }   else if (num < 0){
    return num
    }   else {
    return 0
    }
 }

let numBers = makeNegative(0.12)

// console.log(numBers);
